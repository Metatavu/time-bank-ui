/**
 * Values for filtering scopes
 */
export enum FilterScopes {
  WEEK = "week",
  DATE = "date",
  MONTH = "month",
  YEAR = "year"
}

/**
 * Values for date formats
 */
export enum DateFormats {
  DATE = "dd/MM/yyyy",
  MONTH = "MM/yyyy",
  YEAR = "yyyy"
}